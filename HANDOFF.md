# 대구 날씨 서비스 인수인계

## 1. 목표
대구 날씨를 일상적으로 확인하는 깔끔한 서비스와 과제 4 검증을 함께 제공한다. 공공데이터포털의 기상청 API를 조사하고 읍면동 선택·예보·특보 등 확장을 준비한다.

## 2. 현재 상태
메인은 `public/index.html`, `service.mjs`, `service.css`이며 현재·24시간·7일 날씨만 보여준다. 과제 기능은 `public/review.html`에서 기존 `app.mjs`를 사용한다. 실데이터 엔진·공식 fixture·수집 규칙은 유지했다. 메인·검증 화면과 전체 커밋 소스의 무인증 접근, 배포 파일 13개 SHA-256 일치를 확인했습니다.

실제 보관값은 9월 22일 21.9°C와 23일 25.8°C, 차이 +3.9°C. 공개 일별 최근값 표는 25.5→25.8°C로 +0.3°C. 둘 모두 검증 화면에서 구분한다. 플랫폼 영수증은 미확인이다.

## 3. 실행
Node.js 22 이상. `npm start`, `npm test`, `npm run build`. 실제 원천 수집은 `npm run collect`. localhost:8004에서 메인, /review.html에서 과제 검증. 공개 배포 원본은 public/, 출력은 dist/.

## 4. 검증
자동 32개, 로컬 메인12·검증27·원자료4개 통과. 공개에서도12+27+4개 통과. 자료와 원문 해시는 docs/verification/에 있다. 최신 제출 소스는 docs/SUBMISSION.md 참고.

## 5. 남은 일
공공데이터포털 실제 API 연동은 하지 않았다. 사용자는 개인키와 서비스별 키를 보유한다고 했지만 이번 요청은 API 조사·선정이다. keys를 채팅으로 요청하지 않는다. 비공개 로컬 설정 파일 .env.kma.local은 준비만 했으며 Git에서 제외된다. 플랫폼 t04_day 영수증과 실제 제출 여부도 아직 미확인이다.

## 6. 다음 행동
docs/PUBLIC-API-PLAN.md의 단기·현재특보·중기를 우선 연결하고 미세먼지/자외선을 보조로 구성한다. 서버 환경변수로 키를 사용하며 실제 응답과 승인 서비스가 확인된 기능만 노출한다. 읍면동은 격자, 중기는 권역/도시, ASOS는 관측소로 표시한다. ASOS 포털 시간자료는 전일까지여서 현재 온도에 쓰지 않는다.

## 7. 보존할 것
Open-Meteo 두 날짜 원자료·보관값과 공식 합성 fixture 원본을 보존한다. 기상청·새 지역을 기존 어제 대비에 섞지 않는다. 실제 날짜·플랫폼 영수증·사용자 검증을 만들지 않는다. 루트의 사용자가 추가한 공식 ZIP·JSON·압축해제 폴더는 수정하거나 삭제하지 않는다. 공개 코드·응답·오류·로그·원자료 URL에 키를 넣지 않는다.
