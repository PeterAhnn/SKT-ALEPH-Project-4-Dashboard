# 대구 날씨 서비스 인수인계

## 1. 목표
대구 날씨를 일상적으로 확인하는 깔끔한 서비스와 과제 4 검증을 함께 제공한다. 공공데이터포털의 기상청 API를 조사하고 읍면동 선택·예보·특보 등 확장을 준비한다.

## 2. 현재 상태
2026-09-23 확장: 상단에 기상 영상·생활 날씨·관측 기록·대구공항 메뉴를 추가했다. `public/explore.*`, `explore-client.mjs`, `lib/weather-extras.mjs`, `portal-source.mjs`를 사용한다. 정확한 적용·보류 범위와 항공 API 제한은 `docs/EXTENDED-SERVICES.md`를 먼저 읽는다. 아래 기본 연동 당시의 미구현 목록보다 이 확장 문서가 우선한다.

메인은 `public/index.html`, `service.mjs`, `service.css`이며 기상청 실황·단기·중기·전국 특보와 에어코리아 대기질을 보여준다. 과제 기능은 `public/review.html`에서 기존 `app.mjs`를 사용한다. 실데이터 엔진·공식 fixture·수집 규칙은 유지했다. 메인·검증 화면과 전체 커밋 소스의 무인증 접근, 새 연동 배포 파일 14개 SHA-256 일치를 확인했습니다.

실제 보관값은 9월 22일 21.9°C와 23일 25.8°C, 차이 +3.9°C. 공개 일별 최근값 표는 25.5→25.8°C로 +0.3°C. 둘 모두 검증 화면에서 구분한다. 플랫폼 영수증은 미확인이다.

## 3. 실행
Node.js 22 이상. `npm start`, `npm test`, `npm run build`. 실제 원천 수집은 `npm run collect`. localhost:8004에서 메인, /review.html에서 과제 검증. 공개 배포 원본은 public/, 출력은 dist/.

## 4. 검증
새 연동 자동 42개, 합성 메인 PC/모바일 각각 18개, 검증 회귀 27개 통과. 이전 Open-Meteo 메인 자동 32개, 로컬 메인12·검증27·원자료4개 통과. 공개에서도12+27+4개 통과. 자료와 원문 해시는 docs/verification/에 있다. 최신 제출 소스는 docs/SUBMISSION.md 참고.

## 5. 남은 일
읍면동 선택은 미구현이다. 대구 CCTV 지점은 공식 목록에서 확인하지 못해 보류했다. TAF와 공항 해설은 RKTN 조회에 자료가 없어 상태를 명시한다. 실제 항공편 운항표는 승인된 API 목록에 없으므로 포함하지 않는다. 에어코리아의 간헐적 실패도 완전 안정화로 기록하지 않는다. 플랫폼 t04_day 영수증과 실제 제출 여부는 미확인이다.

## 6. 다음 행동
`docs/EXTENDED-SERVICES.md`의 적용·보류 범위를 먼저 확인한다. 이미 연동한 기상 영상·생활지수·태풍·영향·ASOS·대구공항을 다시 구현하지 않는다. 사용자는 2026-09-23 별도 인증키가 필요한 API허브를 사용하지 말라고 확정했다. Production 키는 Vercel 서버 환경변수에만 있다. Git 제외 `.env.kma.local`은 비어 있으며 로컬과 Vercel은 자동 동기화되지 않는다. 키를 채팅으로 요청하지 않는다. 관측소·공항·격자·권역을 구분하고 추가 원천을 과제 기존 두 날짜 비교에 섞지 않는다.

## 7. 보존할 것
Open-Meteo 두 날짜 원자료·보관값과 공식 합성 fixture 원본을 보존한다. 기상청·새 지역을 기존 어제 대비에 섞지 않는다. 실제 날짜·플랫폼 영수증·사용자 검증을 만들지 않는다. 루트의 사용자가 추가한 공식 ZIP·JSON·압축해제 폴더는 수정하거나 삭제하지 않는다. 공개 코드·응답·오류·로그·원자료 URL에 키를 넣지 않는다.
